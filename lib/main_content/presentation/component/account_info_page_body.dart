import 'package:flutter/material.dart';

class AccountInfoPageBody extends StatelessWidget {
  const AccountInfoPageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [
          // change image


        ],
      ),
    );
  }
}

